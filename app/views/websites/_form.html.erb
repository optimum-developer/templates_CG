<%= form_with(model: website, local: true) do |form| %>
  <% if website.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(website.errors.count, "error") %> prohibited this website from being saved:</h2>

      <ul>
      <% website.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="row">
      <div class="col-lg-12">
        <div class="form-group">
          <label for="Select">Select Bussiness</label>
          <select id="Select" class="form-control" name="website[category]">
              <option value="1" >Blog Website</option>
              <option value="2" >Restaurant</option>
              <option value="3" >School</option>
          </select>
        </div>
      </div>
    </div>

  <div class="actions">
    <%= form.submit  class: "btn btn-default" %>
  </div>
<% end %>
